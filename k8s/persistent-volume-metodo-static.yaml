apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv1
spec:
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteOnce #Pode gravar e ler desde q esteja no mesmo node    
  #storageClassName: local-device